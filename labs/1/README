# Lab 1  - Prefix sums lab Exercise
Solution by Christian Watson for CSE 3150

## Usage

Execute the following commands to compile and run the tests:

``` bash
g++ unit_tests_prefix_sum.cpp prefix_sum.cpp -o lab1
./lab1
```
